apiVersion: apps/v1
kind: Deployment
metadata:
  name: recipe-adapters
  namespace: greenbowl
spec:
  replicas: 2
  selector:
    matchLabels:
      app: recipe-adapters
  template:
    metadata:
      labels:
        app: recipe-adapters
    spec:
      containers:
        - name: recipe-adapters
          image: hellmir/greenbowl-recipe-adapters:1.0.0
          ports:
            - containerPort: 8082
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres.greenbowl.svc.cluster.local:5432/greenbowl"
            - name: SPRING_DATASOURCE_USERNAME
              value: "postgresuser"
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_PASSWORD
