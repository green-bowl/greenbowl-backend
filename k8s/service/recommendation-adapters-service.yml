apiVersion: v1
kind: Service
metadata:
  name: recommendation-adapters
  namespace: greenbowl
spec:
  selector:
    app: recommendation-adapters
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer
