apiVersion: v1
kind: Service
metadata:
  name: user-adapters
  namespace: greenbowl
spec:
  selector:
    app: user-adapters
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
  type: LoadBalancer
